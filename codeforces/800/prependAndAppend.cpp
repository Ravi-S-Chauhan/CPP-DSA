#include<iostream>
using namespace std;

void tc(){
    int n;
    string s;
    cin>>n;
    cin.ignore();
    getline(cin,s);
    
}

int main(){
    ios_base::sync_with_stdio(false); cin.tie(0);
    int t;
    cin>>t;
    while(t--) tc();
    return 0;
}